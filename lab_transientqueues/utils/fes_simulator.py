class FESSimulator():
    def __init__(self, log_path, max_length):
        self.log_path = log_path
        self.max_length = max_length

    def log_fes_header(self, filename):
        with open(f"s", "w")

    def log_fes_events(self, filename):
        pass